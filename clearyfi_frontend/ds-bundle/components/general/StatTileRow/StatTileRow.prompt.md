StatTileRow from @clearyfi/design-prototype. Use via `window.ClearyFiDS.StatTileRow` (bundle loaded from the root `_ds_bundle.js`).

Auto-fitting row for `StatTile`s — wraps to as many columns as the container allows.

## Props

```ts
interface StatTileRowProps {
  /** `StatTile`s. */
  children: React.ReactNode;
  className?: string;
}
```
