import * as React from 'react';

/**
 * StatTileRow — from @clearyfi/design-prototype@0.1.0.
 */
export interface StatTileRowProps {
  /** `StatTile`s. */
  children: React.ReactNode;
  className?: string;
}

export declare const StatTileRow: React.ComponentType<StatTileRowProps>;
