# P4 — Qualitative view v2 · Operator manual-verification questionnaire

**Purpose:** the hands-on gate after the QA report (`4-qa.md`). QA verified this by automated +
scripted driving; this questionnaire is for **the operator** to confirm the *felt* behaviour in a
real browser and sign off.

- **Change:** frontend-only, honest Track-2 **placeholder** expansion of the Qualitative view.
- **Branch:** `sector-v2-qualitative` (not committed).
- **QA verdict:** ✅ PASS — accepted at QA-tester level (see `4-qa.md`).
- **App used:** http://localhost:8001/sector-analytics (container `secfin-manual`).

**Status: ✅ COMPLETED — operator hand-ran all 13 rows interactively, verdict "Confirmed".**

## The load-bearing rule for this change

Everything on this view is a **placeholder** — Track 2 (free-text narrative) we do not ingest yet.
The one thing that must hold everywhere, including inside every expanded/revealed panel: **no
fabricated data** — no figure, %, count, ●/○ flag, direction word, ticker, company name, or excerpt.
Every data cell must read `—` / `planned` / `to be defined` / `none shown`.

## Checklist — operator results (hand-run 2026-07-24)

| # | Step | Expected result | AC | Result | Notes |
|---|------|-----------------|----|--------|-------|
| 1 | Land on the Qualitative view | Banner "TRACK 2 · NOT YET DERIVED FROM FILINGS" + "structured … free-text narrative … Nothing here is fabricated" | AC-1 | ✅ | Operator noted the far-right sidebar region was empty (Qualitative had no right rail). **Follow-up FIX applied + accepted (2026-07-24):** added a Qualitative right rail (`qualRailHtml` — "Track 2" note + "how to read" card, no data), matching the other views. Operator accepted as-is (theme-name wrapping at ~1280px deemed acceptable). e2e `sectorapp-qual errors=0`. |
| 2 | Read the 7 Risk-factor themes rows | Each: name + empty dashed bar (no fill) + `—` + `PLANNED` chip + `Filings →`; no numbers | AC-2/3 | ✅ | |
| 3 | Click a theme row | Inline panel "…to be defined · no filing text shown"; no excerpt | AC-3 | ✅ | |
| 4 | Click the same row again | Panel closes; row returns to collapsed (idempotent) | AC-10 | ✅ | |
| 5 | Click "Filings →" on any row | Nothing happens — no navigation, no error | AC-7 | ✅ | Inert stub (P5 not built). |
| 6 | Read the 3 side cards | Emerging / Going-concern / Material litigation, each "to be defined · no filers shown" | AC-5 | ✅ | Present (resolves row-1's "right column" question — the cards render; only the page-level rail is absent). |
| 7 | Read the Per-filer signals matrix | 5 headers over an empty body — no fabricated rows | AC-5 | ✅ | |
| 8 | Click "Reveal the filers behind these signals" | "…none shown. No tickers are fabricated." — no ticker list | AC-6 | ✅ | |
| 9 | Scroll to Disclosure landscape (02) | Exactly 7 blocks (Cyber, CAMs, Auditor, RF-volume, Non-GAAP, Late/deficient, Human-capital), each title + SEC-source line + "to be defined" body | AC-4 | ✅ | |
| 10 | Click "Reveal the filers" on Cyber / Auditor / Late | Each opens "…none shown…"; the other 4 blocks have no reveal button | AC-6 | ✅ | |
| 11 | Keyboard: Tab to a row, press Enter/Space | Row expands (like a click) with a visible focus ring | AC-3 | ✅ | |
| 12 | Honesty scan with panels open | No figure/%/●/direction word/ticker/excerpt anywhere; closing "Nothing … derived … or estimated." | AC-2 | ✅ | |
| 13 | Switch Sector / Company / Compare / back to Qualitative | Each renders; selection state preserved | AC-9 | ✅ | |

## Overall sign-off

- **Verdict:** ☑ **Confirmed — matches expected** · ☐ Accepted at QA-tester level · ☐ Defect found
- **Operator:** tijesunimi-peters  **Date:** 2026-07-24
- **Notes:** All 13 rows matched expected behaviour, hand-run interactively on the live :8001 build.
  One observation on row 1 — the far-right sidebar region is empty — was confirmed **by design**
  (the Qualitative view intentionally has no right rail per the v2 prototype), so it is **not** a
  defect. (Optional future polish, out of P4 scope: if the empty far-right ever reads as unbalanced,
  a later tweak could widen/center the Qualitative content or add a rail — operator's call.)

---
*Generated by the QA gate after `4-qa.md`, completed by the operator. Confirmed → the change is
ready for a deploy request (`/devops-engineer`, operator-gated).*
